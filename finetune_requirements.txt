# Additional requirements for GAN fine-tuning
# Install with: pip install -r finetune_requirements.txt

# Core dependencies (if not already installed)
tensorflow>=2.8.0
numpy>=1.21.0
matplotlib>=3.5.0
music21>=8.1.0

# Optional for enhanced functionality
tqdm>=4.64.0          # Progress bars
scipy>=1.8.0          # Signal processing utilities
librosa>=0.9.0        # Audio analysis (optional)
pretty_midi>=0.2.9    # Alternative MIDI processing (optional)

# Development tools (optional)
jupyter>=1.0.0        # For experimentation
seaborn>=0.11.0       # Enhanced plotting
